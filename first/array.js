var knowledge = [];
// the above is the syntax for declaring an array
var test = []; //another way to declare array in ts
var allUser = []; //another way of defining an array in a variable created by an object
var models = [
    [255, 255, 255]
    // [""] this string entry is not possible as we declared it as number inside array also
];
knowledge.push("github");
knowledge.push("aws");
test.push(1);
allUser.push({ name: "github", isActive: true });
